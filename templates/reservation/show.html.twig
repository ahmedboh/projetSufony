{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}
{% block stylesheets %}
<link rel="stylesheet" href="{{asset('assets/style6.css')}}">
{% endblock %}
{% block body %}
<div style="width :50%;float:right">
<br>
    <h1 style="color: tomato;font-weight: bold;" >Reservation</h1>
<br>
<br>
    <table class="table" >
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>DateR</th>
                <td>{{ reservation.dateR ? reservation.dateR|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ reservation.etat }}</td>
            </tr>
            <tr>
                <th>Nom client</th>
                <td>{{ reservation.idCli.nomCli }}</td>
            </tr>
            <tr>
                <th>Libelle service</th>
                <td>{{ reservation.idService.Libelle }}</td>
            </tr>
        </tbody>
    </table>
<br>
    <a style="position:absolute; top:110px;right:130px"  class="btn btn-outline-success " href="{{ path('reservation_index') }}">Retourner à la liste</a>

    <a style="float:left;margin-left: 0px;margin-right: 180px" class="btn btn-warning " href="{{ path('reservation_edit', {'id': reservation.id}) }}">editer</a>

    {{ include('reservation/_delete_form.html.twig') }}
    <div>
{% endblock %}
