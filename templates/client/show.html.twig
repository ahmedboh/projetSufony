{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}
{% block stylesheets %}
<link rel="stylesheet" href="{{asset('assets/style3.css')}}">
{% endblock %}
{% block body %}
    <br>
    <div  >
    <h1 style="color: tomato;font-weight: bold;" >Client</h1>
      
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>NomCli</th>
                <td>{{ client.nomCli }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ client.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ client.adresse }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ client.email }}</td>
            </tr>
        </tbody>
    </table>
     <br>
    <a style="position:absolute; top:100px;right:600px" href="{{ path('client_index') }}"class="btn btn-outline-success ">Retourner à la liste </a>

    <a style="float:left;margin-left: 0px;margin-right: 330px" class="btn btn-warning " href="{{ path('client_edit', {'id': client.id}) }}">editer</a>

    {{ include('client/_delete_form.html.twig') }}
    </div>
{% endblock %}
